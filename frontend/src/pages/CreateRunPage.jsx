function CreateRunPage() {
  return (
    <section className="page-card">
      <p className="page-kicker">Protected page</p>
      <h2>Create Run Page</h2>
      <p>
        This page will hold the run creation form and submit the new run to the
        backend.
      </p>
    </section>
  );
}

export default CreateRunPage;
