function RunsPage() {
  return (
    <section className="page-card">
      <p className="page-kicker">Protected page</p>
      <h2>Runs Page</h2>
      <p>
        This page will display the compact runs list with filtering and quick
        access to each run detail page.
      </p>
    </section>
  );
}

export default RunsPage;
