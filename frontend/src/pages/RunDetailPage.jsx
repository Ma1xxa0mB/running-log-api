function RunDetailPage() {
  return (
    <section className="page-card">
      <p className="page-kicker">Protected page</p>
      <h2>Run Detail Page</h2>
      <p>
        This page will focus on one precise run with full metrics, session
        detail, and time in heart-rate zones.
      </p>
    </section>
  );
}

export default RunDetailPage;
